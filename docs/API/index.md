Within the Atena package, the API endpoints are included but are specific to the [Atena Frontend](https://github.com/weaviate/elysia-frontend). 

However, there are a range of functionalities included that can be useful for using Atena in an app environment.